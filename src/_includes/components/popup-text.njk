<{{element}} class="popup-text {{ classnames }}" aria-label="{{ lines | join(' ') }}">
  {% for line in lines %}
  <span class="popup-text__block" aria-hidden="true">
    <span class="popup-text__block-inner">{{ line }}</span>
  </span>
  {% endfor %}
</{{element}}>
